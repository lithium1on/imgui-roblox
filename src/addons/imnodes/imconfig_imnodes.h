// Dear ImGui configuration for imnodes: imconfig_roblox.h plus Dear ImGui's file helpers, which imnodes' .ini *file*
// functions call. Those functions are never linked (only the RBX_EXPORT functions are kept), and the setting only
// changes declarations, not the layout of any struct shared with the bundle.
#pragma once

#include "imconfig_roblox.h"

#undef IMGUI_DISABLE_FILE_FUNCTIONS
